
##Configuration Docker (optionnel)
#version: '3.8'
services:
  minio:
    image: minio/minio:latest
#    image: minio/minio:RELEASE.2024-03-05T04-48-44Z
#    image: 'minio/minio:latest'
    container_name: minio
    ports:
      - "9000:9000"   # Port de l'API S3 (Pour Spring Boot)
      - "9001:9001"   # Port de la Console Web (Pour votre navigateur)

#      - '9000:9000'
#      - '8900:8900'
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    volumes:
      - 'minio_data:/data/minio'
    command: 'minio server /data/minio --console-address ":9001"'
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3

    restart: unless-stopped

volumes:
  minio_data:
    driver: local





    ##Configuration Docker (optionnel)

#services:
#  minio:
#    image: minio/minio
#    container_name: minio
#    ports:
#
#      - '9000:9000'
#      - '9001:9001'
#    environment:
#      MINIO_ROOT_USER: minioadmin
#      MINIO_ROOT_PASSWORD: minioadmin
#    volumes:
#      - 'minio_data:/data'
#    command: 'server /data --console-address ":9001"'
#    healthcheck:
#      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
#      interval: 30s
#      timeout: 20s
#      retries: 3
#
#    restart: unless-stopped
#
#volumes:
#  minio_data:
#    driver: local
